

# docker compose build

# docker compose down
# docker compose up
# docker compose exec  -it ubuntu-basic /bin/bash
# docker compose  exec ubuntu-basic sh
# docker compose  exec ubuntu-basic bash
# curl -fsSL https://cli.devin.ai/install.sh | bash

# https://docs.devin.ai/cli
services:



  ubuntu-basic:
    image: ubuntu:latest
    build:
      context: .
      dockerfile: ./docker/Dockerfile.devin.yml
    command: >
      /bin/bash -c "
      echo 1;
      while true; do
        echo "Running..."
        sleep 1  # Pauses for 1 second between iterations
      done
      "
    volumes:
      - /data/.agents:.agents